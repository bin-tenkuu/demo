<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="demo.mapper.SysRoleMenuMapper">
    <insert id="insertRoleMenu">
        insert into sys_role_menu(role_id, menu_id)
        values
        <foreach collection="menuIds" item="menuId" separator=",">
            (#{roleId}, #{menuId})
        </foreach>
    </insert>

    <delete id="deleteByMenuIds">
        delete
        from sys_role_menu where menu_id in
        <foreach collection="ids" item="menuId" open="(" separator="," close=")">
            #{menuId}
        </foreach>
    </delete>

    <delete id="deleteByRoleId">
        delete
        from sys_role_menu
        where role_id = #{roleId}
    </delete>

    <delete id="deleteByRoleMenu">
        delete
        from sys_role_menu
        where role_id = #{roleId}
          and menu_id in
        <foreach collection="menuIds" item="menuId" open="(" separator="," close=")">
            #{menuId}
        </foreach>
    </delete>

    <select id="selectMenuByRoleId" resultMap="demo.mapper.SysMenuMapper.BaseResultMap">
        select id, menu_name, parent_id, order_num, path, component, query, is_frame, is_cache, menu_type, visible,
            perms, icon
        from sys_role_menu
                 inner join sys_menu on menu_id = id
        where status = '0'
          and role_id = #{roleId}
    </select>
</mapper>
